// import WebpackDevServer from 'webpack-dev-server'
// import settings from '../../../../../../../config/webpack/settings'
// import { default as MainServer } from '../../src/server/server'
// import { default as config } from '../../src/server/middleware/config/core.config'

const startAppsAction = async () => {
  // Load available components from federated folder
  // Display available components in selector list
  // Based on selection start each component on consecutive port
  // const server = new WebpackDevServer(settings.devServer, clientCompiler)
  // const config = require('../../src/server/middleware/config/core.config').default
  //
  // // Start server
  // await server.start()
  // const baseServer = new BaseServer(config)
  //
  // baseServer.createApp(server.app).bootstrap({
  //   dalmatianConfig,
  //   assetPath: '/assets',
  // })
}

// AvailabilityChecker: http://localhost:8001

export default startAppsAction
